# Kubernetes Logs MCP Server

An MCP (Model Context Protocol) server that provides tools and prompt to investigate incidents.
The local operator should have configured and have access to the target kubernetes cluster where the investigation should happen. 
